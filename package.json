{
  "name": "venue",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=18" },
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "test": "pnpm contracts:build && vitest run",
    "test:watch": "vitest",
    "typecheck": "pnpm -r exec tsc --noEmit",
    "contracts:deps": "tsx scripts/ensure-foundry-deps.ts",
    "contracts:build": "tsx scripts/ensure-foundry-deps.ts build",
    "contracts:test": "tsx scripts/ensure-foundry-deps.ts test",
    "probe": "tsx scripts/probe-real-policy.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@venue/cleanverse": "workspace:*",
    "@venue/core": "workspace:*",
    "fast-check": "^3.23.1",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "viem": "^2.52.2",
    "vitest": "^2.1.8"
  }
}
