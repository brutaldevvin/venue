node_modules/
dist/
.next/
out/
.env
.env.*
!.env.example
contracts/out/
contracts/cache/
contracts/broadcast/
# Vendored dependencies. Restore with:
#   cd contracts && forge install foundry-rs/forge-std OpenZeppelin/openzeppelin-contracts@v5.1.0
contracts/lib/
*.log
.DS_Store
.venue-makers.json

# logo.PNG is the source lockup and is committed; brand/ and the console's public/ assets
# are derived from it by scripts/build-brand.py.

# Internal planning notes and the working engineering log, not part of the deliverable.
# EVIDENCE-RUN.md is the generated, shippable record and is committed.
BUILD-VENUE.md
VENUE.md
venue-design.md
EVIDENCE.md
fly-secrets.sh
